import { AdminReportsPage } from "@/features/admin/components/reports/admin-reports-page";

/**
 * Trang (Page) Page
 * 
 * Chức năng: Định nghĩa giao diện tuyến đường (Routing Page) cho hệ thống AutoWash Pro.
 * Đường dẫn tương đối: src/app/app/admin/reports/page.tsx
 */
export default function Page() {
  return <AdminReportsPage />;
}
