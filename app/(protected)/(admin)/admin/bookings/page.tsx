import { AdminBookingsPage } from "@/features/admin/components/bookings/admin-bookings-page";

/**
 * Trang (Page) Page
 * 
 * Chức năng: Định nghĩa giao diện tuyến đường (Routing Page) cho hệ thống AutoWash Pro.
 * Đường dẫn tương đối: src/app/app/admin/bookings/page.tsx
 */
export default function Page() {
  return <AdminBookingsPage />;
}
