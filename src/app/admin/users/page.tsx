import { AdminUsersPage } from "@/components/admin/users/AdminUsersPage";

/**
 * Trang (Page) Page
 * 
 * Chức năng: Định nghĩa giao diện tuyến đường (Routing Page) cho hệ thống AutoWash Pro.
 * Đường dẫn tương đối: src/app/app/admin/users/page.tsx
 */
export default function Page() {
  return <AdminUsersPage />;
}
